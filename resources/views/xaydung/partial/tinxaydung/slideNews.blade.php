<section class="slider-projects">
    <div class="slider-projects__inner">
        @if(!empty($thisSlideNews))
            {!! $thisSlideNews !!}
        @else
            <h3>Không tìm thấy nội dung</h3>
        @endif
    </div>
</section>